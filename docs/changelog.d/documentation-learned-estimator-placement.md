- **eisv contract:** a dated section places fitted or outcome-learned estimators (fitted blend weights, a learned readout) in the efficacy, label-gated bucket the ROADMAP already defines, and summarises, pointing to it as the authority, the reopening-clause clarification ratified in the registered outcome stop rule the same day (see the Changed entry): an estimator fitted to the same trusted task/test label channel does not by itself constitute a new premise. Label-free work (self-predictability confidence, windowed baselines, system identification) may proceed offline; any live-path change waits for the 2026-12-01 report. The section carries a dated deployment snapshot of how many identities run on the ODE fallback alone, with its read-only query committed as `scripts/analysis/ode_fallback_reach_snapshot.sql`, and marks as an inference its use of the 2026-09-23 instrument-supply read. The sensory-split table now anchors on function names instead of stale line numbers, and its V row states that the sensor's `_compute_V` reaches the ODE only through spring coupling, while the verdict V is an EMA of `E_obs − I_obs`. One line mirrors the placement into `docs/ROADMAP.md` and a pointer into the maths roadmap. Documentation plus one read-only query file: no read, estimator, weight or threshold changes, and the outcome question stays unresolved. (#2440)
