- **check-ins:** compact, standard and minimal `sync_state` responses, and mirror's actionable margin, now carry `unmeasurable_edges` and `margin_scope` beside `margin` when an edge went unassessed. The formatter used to drop them, so an agent whose coherence edge is unassessed (every agent today) read `margin: "comfortable"` with nothing saying which edges it covered. The envelope already lifted both fields into `state_summary`; they now arrive. Fully assessed margins add nothing.
