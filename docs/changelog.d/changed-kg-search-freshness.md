- **knowledge search:** the staleness warning's version clause is retired (operator decision, 2026-09-26). It is the "2+ minor versions behind current" rule under *Staleness warnings* in the 2026-03 Knowledge Graph entry. It compared two VERSION-file values, so it counted release cuts, not code change: it fired on entries 22-57 days old depending on when releases were cut, and never on a `leave_note`, which records no version. `staleness_warning` is now the last-write age clause alone (more than 60 days, open entries, threshold unchanged), and its wording follows the entry: a durable entry (the lifecycle's permanent types and tags, plus `insight`) reads "Last written N days ago; durable entries can carry volatile details (ports, paths, refs) — verify before acting on them" instead of "still open". Because that classification is the retention one, an open bug tagged `architecture` also gets the neutral wording. `system_version` stays on results as store-time provenance, and the dashboard's stale flag still reads `staleness_warning`, so it now counts fewer entries. Search rows gain `last_activity_days` beside the warning: days since the last write, the name `knowledge(action="audit")` already uses for that quantity. The default `search_shared_memory` digest carries `last_activity_days` instead of the warning sentence and explains it once in `state_summary.staleness_note`. Where the AGE supersession edge exists, it names a superseded row's replacement as `superseded_by` (first successor id), and the single digest kept by budget compaction keeps `status`, `superseded_by` and `last_activity_days`. It states `confidence_note` once, at the top level; the note was also copied into `state_summary`. `knowledge(action="details")` on a superseded entry now carries `superseded_by` and `superseded_warning` where the AGE edge exists. Inside the 3,000-byte digest budget, optional coaching gives way before any attribution is withheld: the tier ladder (`response_options`) first, then the extra retrieval options, then a truncated digest's `expand_with`, which repeats `raw_governance_hint`. Each piece yields only if attribution still needs the room, and comes back if it fits once attribution is restored. The `include_details` override disclosure (`requested_tier`, `details_omitted_by`) never yields to attribution. Withheld attribution now always carries `digest_attribution_omitted` when a digest is shown, and the truncation markers are measured inside the budget instead of added after it (#TBD).
