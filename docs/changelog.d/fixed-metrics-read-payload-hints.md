- **check_working_state and size/next-step hints:** the default read says each fact once, and every hint names something the caller can use (external agent report, 2026-09-25). Measured from the real handler: a provisional default read goes from 3,350 to 1,986 bytes, an uninitialized one from 2,751 to 1,586, and `include_state=true` from 7,145 to the default's 1,986. `include_state` only made the envelope attach the whole canonical payload (the nested state it described surfaced only when state interpretation failed), so the envelope no longer escalates on it, the handler no longer requests that state, and its description now says it is retained for compatibility with no effect (the in-repo `ollama_bridge` passes `verbosity="standard"` instead). The default envelope gives E, I, S, V and `risk_score` as bare values (the per-field contract is on the tool description and the risk band in `risk_summary`), trims the coherence badge to sync_state's `{value, status, source, role}` shape so the "not health-rated" disclosure stays inline, drops `legacy_diagnostics` when it only repeats that badge (before the first check-in the badge reads "pending", so the block stays), drops `action_summary.reason` when it is the verdict's `meaning` or the payload's guidance, and teaches the tier ladder once, in `raw_governance_hint` (before the first check-in it names only `verbosity='full'`, since no tier has basin or mode yet); `response_options` still reports the tier on `standard` and `full`. The canonical `get_governance_metrics` minimal shape is unchanged. Every tier now carries `next_action`, which `unitares.lifecycle-envelope.v1` requires and the default and full reads lacked (the conformance test hand-supplied it); it comes from the payload's own next step, then, for a paused or rejected agent, the step that follows that decision (interpreted guidance does not know the decision), then the interpreted guidance or the verdict's recommended step, and `state_summary` no longer repeats a second one. A real read on a binding the server only inferred (reachable through nested `use_tool` dispatch) is now marked with a compact `identity_assurance` (`tier`, `caller_proven: false`, `session_source`), as the tool description promises; caller-proven reads are unchanged. `verdict_caveat` names only a path present in the response: `state_summary.evidence` on a metrics read, a `raw_governance` path only when raw is included, and nothing on a bounded `sync_state`, whose basis is already inline (it used to point into an omitted `raw_governance`, and following it meant another check-in). `_response_size.reduce_with` never names the mode in effect (read from the request, not from `response_options`), and `start_session` gets none, since acting on it would mint a second identity; the size receipt stays. `list_tools(lite=true)` carries no `agent_signature` (for a caller-asserted transport-session or `x_client_id` binding its 1,559-1,943 B signature put the 4 KiB handshake at 4,957-5,341 B; a server-inferred binding's already collapsed to `{"uuid": null}`), and its tip names `describe_tool(..., lite=true)` for parameters rather than the 10-17 KB default record. `describe_tool`'s in-process default now matches its advertised `lite=false`; the Wave 3a probe asks for `lite=true` explicitly and serves the same bytes. Interface contract 1.19.0: the `include_state` and `verbosity` descriptions (which promised EISV meanings `standard` never carried) move the two metrics input digests and the surface digest, and `process_agent_update`/`sync_state`'s `response_mode` description now names the degraded-identity and warnings triggers that make `auto` resolve to mirror (#2472).
