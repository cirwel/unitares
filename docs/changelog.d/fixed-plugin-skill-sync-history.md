- **ci(skills):** the plugin skill-sync workflow checks unitares out with full history. On its first live run it refused with exit 4: when the mirror and canonical declare the same verification date, `sync-plugin-skills.sh`'s direction guard decides from canonical's git history, and a depth-1 checkout has none, so it fails closed. `open-plugin-skill-sync-pr.sh` now prints enough of a failed sync's log to include the refusal's reasons, not only its hint. When the mirror is already in sync, it also closes its own sync PR if one is still open (a hand-made sync merged first), deleting the automation branch and saying why in a comment. The shared contract adds the plugin skill mirror to the single-writer surfaces: do not sync it by hand.
