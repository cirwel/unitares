- **federation wording (attestation docs):** the four attestation documents that the 2026-09-25 federation work left alone to avoid conflicts now link the federation trust decision record and follow the wording rule from #2451 (#2454). `docs/EVIDENCE_AND_LIMITS.md` and `docs/SCOPE_AND_THREAT_MODEL.md` no longer describe multi-principal trust as an open track. They say it is not scheduled (D3a) and give the recorded wake condition: a real outside party, a person or organisation with its own administrative root, asks to verify a record from this deployment. The threat model also says that the choice to restore or retire party-HMAC key issuance was decided (D5: retire minting and keep history readable), and that it is implemented in #2449. The `drr.v1` decision packet and its dormant-capability registry row now point to the record for reason 4, the second principal. Both keep all five wake preconditions as they were and state that citing the record does not satisfy the non-exportable custody criterion. Where "federation" meant several authority domains, these files now say "cross-operator" or "multi-principal". The evidence page's section heading becomes "Local control and cross-operator trust", and its test pin moves with it. The note in `docs/dev/CANONICAL_SOURCES.md` that called these files pending now says they are done. The single-operator sense keeps "federation kernel". No decision, code, flag, schema or deployment changes.
