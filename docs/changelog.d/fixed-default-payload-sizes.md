- **response size:** three default calls returned far more than a caller asked for (measured on a live server, 2026-09-25). `dialectic(action="list")` was 173 KB because every row carried its full `topic` (up to ~12 KB). List rows now carry a 280-character preview with `topic_truncated` and `topic_chars`, while `get` and `include_transcript=true` still return the full text; the dashboard's Dialectic tab gets a "full topic" expander that fetches it. A bare `skills()` returned every skill's full markdown (100 KB for seven skills). It now returns an index with `content_omitted: true`, and `name=` and `since_version=` still return content, which is how the plugin adapter fetches. `agent(action="list")` / `list_agents` never applied the lite defaults it documents (20 agents, last 7 days): the schema passes `limit` and `recent_days` as explicit `None`, which bypassed the handler's fallbacks, so every default call built the whole roster (3,974 agents), the response serializer cut it to 100 arbitrary rows plus a `"... (N more items)"` string, and `shown` still said 3974. The defaults now apply, lite pages are capped at the serializer's 100 so `shown` matches the rows sent, and full mode with no `limit` returns 100 so `summary.returned` is true. Explicit full-mode limits are unchanged.
