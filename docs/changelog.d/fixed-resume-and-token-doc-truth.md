- **identity docs:** `onboard`'s `resume` description, which is also on the `start_session` alias, no longer lists `continuity_token`, `agent_id` and `name` as resume signals. A name is never looked up (name-based reconnection was removed 2026-04-17), and a `continuity_token` without `force_new` is refused by S1-c. The text now says that resume reuses the identity already bound to this session; that passing `name` or `agent_id` still counts as a proof signal for S13, so it resumes the session's existing binding instead of minting; and names the `identity(agent_uuid, continuity_token, resume=true)` rebind. The lifecycle skill no longer says S1-a accepts token resume during a deprecation window. Interface contract 1.18.0: descriptions only, no parameter changes.
