- **federation decisions:** a new decision record, `docs/proposals/active/federation-trust-decisions-2026-09-25.md`, records three decisions taken on 2026-09-25 under the operator's delegation, after an adversarial design review. Multi-principal trust is not scheduled: as of that date there is no second principal and no external verifier, and a second host run by the same operator is not one. It wakes when a real outside party asks to verify a record from this deployment. Custody for the `drr.v1` receipt key is deferred behind that, and the record keeps the framing for whoever wakes it. Minting of v2 party-HMAC signatures on dialectic resolutions is retired going forward, with history kept readable and the record's fields unchanged; implementation is a follow-up tracked in #2449. The roadmap moves multi-principal trust from "Now" to "Later" as not scheduled, with its wake condition, and the identity plan's S20 entry gains a dated pointer answering its external-verifier question. No code, flag, schema or deployment change.
