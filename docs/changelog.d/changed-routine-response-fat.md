- **responses:** routine responses say each fact once. A caller-proven strong `agent_signature` keeps its role declarations, `continuity_claim` and assurance tier but drops the nested blocks that restated its flat fields (1,969 B → 556 B on every success response). A default `start_session` lifts `agent_id`, `display_name`, `is_new` and `continuity_token` and no longer repeats the onboard record under `raw_governance` (2,903 B → 1,030 B); `response_mode="full"` restores it. A clean `sync_state` proceed drops the repeated action, verdict, risk and health fields but keeps every field the SDK reads. Weak or unproven bindings, rejected provenance values, and guide, pause and provisional verdicts keep their full shape. `tests/test_response_budgets.py` pins byte budgets for both lifecycle responses.
