- **responses:** routine responses drop what restates the normal case and mark it with `response_shape: "routine"`; anything unusual keeps its full shape. A plain fresh `start_session` (outcome `minted_force_new`/`minted_fresh`, no resume, reactivation, lineage or trajectory) no longer repeats the onboard record under `raw_governance`; it lifts `identity_resolution_outcome`, a compact `identity_assurance` (tier, session_source, caller_proven), and `continuity_token` grouped under `rebind` with its use caveat. A clean `sync_state` proceed (positive evidence of healthy, comfortable margin and a safe verdict) drops the repeated approve/safe/healthy/comfortable values and keeps `action_summary.action`, `reason` and `risk_score` plus every field the SDK reads. On responses that carry `agent_signature` (canonical tools and non-envelope tools such as `list_tools`), a caller-proven strong signature with a routine continuity claim keeps its role declarations, assurance and `continuity_claim` but drops the nested blocks restating its own flat fields (`detail: "compact"`); weak, unproven, UUID-direct, discontinuous or rejected-provenance signatures keep the full record. `tests/test_response_budgets.py` pins byte budgets on the normal case.
