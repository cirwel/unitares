- **tool surface:** the progressive `tools/list` every session loads before its first call shrinks from 44,199 to 40,406 bytes (about 950 tokens by the bytes÷4 estimate). `search_shared_memory` stops advertising six fields that only other `knowledge` actions read (`offset`, `epoch_scope`, `scope`, `evidence_ids`, `verification_basis`, `decision_standard`); search never read them and `knowledge` keeps them. The EISV field contract rides once on the advertised surface, on `check_working_state`; `sync_state` and `record_result` point at `describe_tool(tool_name='check_working_state')`, and `describe_tool`'s full view of either still appends the contract (its explicit `lite=true` view now shows the pointer instead). The identity briefs are shorter: `agent_id` reads "UUID; leave unset for yourself." Corrections: `store_finding`'s `agent_id` tells callers to leave it unset (it said "Filter by agent"); six model docstrings that reached the wire as `inputSchema.description` no longer invite an argless `identity()`, promise "supportive feedback", or name a canonical tool on an alias; `list_tools`' `verbose` says it is ignored and `lite` says what it returns. A two-sided ratchet test holds the progressive surface between 39,000 and 41,000 bytes under the default schema policy. Interface contract 1.17.0; merge after #2435 (1.16.0).
