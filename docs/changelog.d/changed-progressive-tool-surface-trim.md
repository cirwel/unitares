- **tool surface:** the progressive `tools/list` every session loads before its first call shrinks from 43,836 to 39,859 bytes (about 1,000 fewer tokens). `search_shared_memory` stops advertising six fields that only other `knowledge` actions read (`offset`, `epoch_scope`, `scope`, `evidence_ids`, `verification_basis`, `decision_standard`); search never read them and `knowledge` keeps them. The EISV field contract rides once on the advertised surface, on `check_working_state`; `sync_state` and `record_result` point at it, and `describe_tool` still returns it in full. The `continuity_token` / `client_session_id` / `agent_id` briefs that ride on all 13 tools are shorter and keep what each is not. Corrections: `store_finding`'s `agent_id` now says it is the writer (it said "Filter by agent"), and `list_tools`' `verbose` says it is ignored and `lite` says what the lite view returns. A ratchet test holds the progressive surface under 41,000 bytes. Interface contract 1.17.0. The remaining bulk is schema structure (null unions and `default: null`, about 10%), which this change does not touch.
