- **identity docs:** the governance-lifecycle skill no longer says S1-a accepts `onboard(continuity_token=...)` during a deprecation window. S1-c refuses it (`status: continuity_token_resume_rejected`); the skill names the `identity(agent_uuid, continuity_token, resume=true)` rebind instead. The `_is_routine_proceed` docstring now matches the code: an unassessed edge stays visible on a routine proceed, and only a near edge or a review nudge disqualifies one.
