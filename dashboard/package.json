{
  "name": "unitares-dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "UNITARES governance dashboard (static client). Tests cover the agent-list render/partition logic.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vitest": "^4.1.9"
  }
}
