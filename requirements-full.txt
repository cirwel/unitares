# Full dependencies for UNITARES Governance MCP
#
# Use this for:
# - HTTP server (`src/mcp_server.py`)
# - multi-client deployments (Cursor + Claude Desktop sharing state)
# - normal local development, tests, and diagnostics
#
# Install:
#   pip install -r requirements-full.txt

-r requirements-core.txt

# HTTP server support (multi-client mode)
uvicorn>=0.35.0,<1.0.0
starlette>=0.37.0,<2.0.0
websockets>=12.0,<18.0

# Dashboard passkey ceremonies
webauthn>=2.8.0,<4.0.0

# Ed25519 operator signatures for request-bound lease attestations and AICs.
cryptography>=41.0.0,<51.0.0

# Operational dependencies (optional but recommended)
psutil>=5.9.0,<8.0.0
aiofiles>=23.2.1,<26.0.0
orjson>=3.9.0,<4.0.0

# Metrics and monitoring
prometheus-client>=0.19.0,<1.0.0

# PostgreSQL backend driver (required for the Postgres-backed server)
asyncpg>=0.29.0,<1.0.0

# Redis runtime continuity support (required in production; demo degrades local-only)
redis>=5.0.0,<9.0.0

# Optional embedding backend for knowledge-graph semantic queries. Not
# installed by default: it requires torch, whose Linux wheel metadata pulls
# cuda-toolkit, cudnn, nccl, nvshmem and triton with no GPU check and no
# opt-out — measured at 5.9 GB installed versus 162 MB without. Semantic search
# falls back to Postgres FTS when it is missing, and the response says so
# (search_degraded). Uncomment here, or install the extra:
#   pip install -e ".[full,embeddings]" -c constraints.txt
# sentence-transformers>=2.2.0,<7.0.0

# Numerical computation (used by governance_monitor, calibration, etc.)
numpy>=1.24.0,<3.0.0

# HTTP client (used by integration tests)
requests>=2.31.0,<3.0.0

# Testing and coverage (optional but included for convenience)
pytest>=7.4.0,<10.0.0
pytest-cov>=4.1.0,<8.0.0
pytest-asyncio>=0.21.0,<2.0.0
hypothesis>=6.0.0,<7.0.0
# Starlette 1.x TestClient backend (httpx 0.x fallback is deprecated).
httpx2>=2.0.0,<3.0.0
fakeredis>=2.38.0,<3.0.0

# Public Pages generator and its regression tests
Markdown>=3.6.0,<4.0.0

# Environment variable loading
python-dotenv>=1.0.0,<2.0.0
